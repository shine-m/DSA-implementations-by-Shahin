#include <bits/stdc++.h>
using namespace std;
#define swap(a, b) a = a + b - (b = a);
#define ln "\n"
int main()
{
    system("cls");
    cout << ln << ln;

    vector<pair<int, int>> edges = {{1, 2}, {3, 2}, {3, 4}, {4, 1}, {5, 6}, {6, 7}, {5, 7}, {8, 9}, {10, 10}};
    int total_nodes = 10;

    vector<int> isvisited(total_nodes + 1, 0);

    cout << ln << ln << ln;
    return 0;
}